import Image from 'next/image'
import { Inter } from '@next/font/google'

const inter = Inter({ subsets: ['latin'] })

const Home = () => {
    return (
        <main>
            <div>HomePage</div>

        </main>
    )
}

export default Home
